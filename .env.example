# .env.example - Template for Environment Variables
# Copy this file to .env and fill in your actual values. Do NOT commit .env to version control.

# TimescaleDB Connection Details
PG_HOST=timescaledb_service
PG_HOST_LOCAL=localhost
PG_PORT_HOST=5433
PG_PORT_CONTAINER=5432
PG_USER=
PG_PASSWORD=
PG_DB=

# SQLAlchemy Database URLs
# These are typically constructed from the variables above and usually don't need to be set directly
# if your application code builds them. However, they are shown here for completeness if used directly.
DATABASE_URL=postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT_CONTAINER}/${PG_DB}
DATABASE_URL_LOCAL=postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST_LOCAL}:${PG_PORT_HOST}/${PG_DB}

# Application Specific (add more as needed)
# EXAMPLE_API_KEY=
